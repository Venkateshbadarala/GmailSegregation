import React from 'react';

import Intro from './css/Intro'

const Login = () => {
    // Function to build the Google OAuth URL
   

    return (
        <div className='flex flex-col bg-gradient-to-r from-gray-900 to-slate-900'>
            <Intro/>
          
        </div>
    );
};

export default Login;
